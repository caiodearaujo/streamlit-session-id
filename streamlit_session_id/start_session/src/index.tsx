import React from "react"
import ReactDOM from "react-dom"
import StartSessionIDComponent from "./StartSessionID"

ReactDOM.render(
  <React.StrictMode>
    <StartSessionIDComponent />
  </React.StrictMode>,
  document.getElementById("root")
)
